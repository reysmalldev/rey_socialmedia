<% if @user.blank? %>
  <%= turbo_stream.append 'add_friend', partial: 'not_found' %>
<% elsif @user.id == Current.user.id %>
  <%= turbo_stream.append 'add_friend', partial: 'self_friend' %>
<% end %>

<%= turbo_stream.update("friendship_#{@user.id}", partial: 'request_sent') if @saved %>
